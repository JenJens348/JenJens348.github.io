const parent = document.getElementsByClassName('div1');

for (const box of parent) {
  div1.addEventListener('click', (event) => {
    event.target.remove();
  });

for (const box of parent) {
  div2.addEventListener('click', (event) => {
    event.target.remove();
  });}

for (const box of parent) {
  div3.addEventListener('click', (event) => {
    event.target.remove();
  });}

  for (const box of parent) {
    div4.addEventListener('click', (event) => {
      event.target.remove();
    });}

  for (const box of parent) {
    div5.addEventListener('click', (event) => {
      event.target.remove();
    });}

  for (const box of parent) {
    div6.addEventListener('click', (event) => {
      event.target.remove();
    });}

  for (const box of parent) {
    div7.addEventListener('click', (event) => {
      event.target.remove();
    });}
  for (const box of parent) {
    div8.addEventListener('click', (event) => {
      event.target.remove();
    });}
  for (const box of parent) {
    div8.addEventListener('click', (event) => {
      event.target.remove();
    });}

  for (const box of parent) {
    div9.addEventListener('click', (event) => {
      event.target.remove();
    });}

  for (const box of parent) {
    div10.addEventListener('click', (event) => {
      event.target.remove();
    });}

}
